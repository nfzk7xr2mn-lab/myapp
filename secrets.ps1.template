# secrets.ps1 - LOCAL ONLY, never commit this file
# Copy to secrets.ps1 and fill in your values.
# Loaded by other scripts via: . "$PSScriptRoot\secrets.ps1"

$ApiKey          = "YOUR-API-KEY-HERE"
$ProxyUri        = "http://localhost:9000/anthropic/v1/messages"
$MyAddresses     = @('your.email@example.com')
$MyDisplayNames  = @('lastname, firstname', 'firstname lastname')

$JiraBaseUrl     = "https://jira.tools.sap"
$JiraEmail       = "your.email@example.com"
$JiraApiToken    = "YOUR-JIRA-API-TOKEN"

$ChefDept        = 'Boss Department Name'
$MitarbeiterDept = 'Your Department Name'
$Mitarbeiter     = @(
    @{ name = 'Boss F.'; email = 'boss@example.com'; dept = $ChefDept; is_chef = $true }
    @{ name = 'FirstName L.'; email = 'first.last@example.com'; dept = $MitarbeiterDept }
)
